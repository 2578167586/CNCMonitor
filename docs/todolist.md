# CNC Studio 企业级改进 TODO

---

## P0 — 致命缺失（影响"能用"）

### 1. 自动化测试覆盖

- [ ] 协议编解码单元测试（`encodeFrame` / `decodeStatusPayload` / `decodeParameterPayload`）
- [ ] CRC-16/MODBUS 校验单元测试（正常帧 / 损坏帧 / 边界情况）
- [ ] ProtocolParser 拆包测试（粘包 / 半包 / 垃圾字节 / 最大包长限制）
- [ ] StorageWorker 批量写入测试（事务成功 / 失败回滚 / 双缓冲交换 / 定时刷盘）
- [ ] TcpClientWorker 断线重连测试（指数退避 / 主动 Stop / 最大重连间隔）
- [ ] MonitorEngine 参数下发防呆测试（范围校验 / 核心参数 RUN 状态拒绝 / 设备不存在拒绝）
- [ ] 集成测试（Simulator → Monitor 端到端数据流）

### 2. 配置文件化

- [ ] 设计 `config.json` 结构（端口、数据库路径、设备数量上限、参数模板、日志级别）
- [ ] 实现 `ConfigManager` 单例类（JSON 读取 / 默认值 / 首次运行自动生成）
- [ ] 替换 SimulatorPage 中的硬编码默认值（First Port / Devices / Hz）
- [ ] 替换 MonitorPage 中的硬编码默认值（Host / First Port / Devices）
- [ ] 替换 MonitorEngine 中的硬编码数据库路径
- [ ] 替换 ParameterTableModel 中的内置参数模板（FeedRate / SpindleLimit / CoolantFlow）

### 3. 部署自动化

- [ ] 运行 windeployqt 自动收集 Qt DLL 依赖（含 Qt5PrintSupport）
- [ ] 编写打包脚本（一键生成完整 `bin/` 部署目录）
- [ ] 验证：在干净机器上解压即运行

---

## P1 — 关键体验（影响"好用"）

### 4. 报警管理完善

- [ ] 报警分级枚举（Info / Warning / Critical）
- [ ] 报警确认机制（未确认 / 已确认 / 已静音状态）
- [ ] 报警列表增加确认按钮和状态列
- [ ] 报警规则配置（报警码与级别的映射）
- [ ] 报警历史查询（按时间范围 / 设备 / 级别过滤）
- [ ] Simulator 增加清除报警按钮

### 5. 数据生命周期管理

- [ ] SQLite 归档策略（30 天前数据移到 `status_history_archive` 表）
- [ ] 定时归档任务（每天凌晨执行）
- [ ] 手动清理入口（UI 设置页或菜单）
- [ ] 数据库文件大小监控与告警阈值

### 6. 日志覆盖关键路径

- [ ] 网络连接失败日志（connectToHost 错误 / 超时）
- [ ] CRC 校验失败日志（记录原始帧数据）
- [ ] 参数下发被拒绝日志（原因 + 参数内容）
- [ ] 数据库写入失败日志（SQLite 错误详情）
- [ ] 仿真启动/停止日志（设备数量 / 端口范围）
- [ ] 日志级别可通过配置文件调整

### 7. 离线/断网完善

- [ ] 断线期间数据补推（重连后补齐缺失时间段）
- [ ] 重连后状态全量同步（避免 UI 残留旧状态）
- [ ] 断线时长统计与告警阈值

---

## P2 — 企业级差异化

### 8. 多语言支持 (i18n)

- [ ] 全局搜索硬编码字符串，用 `QObject::tr()` 包裹
- [ ] 配置 Qt Linguist（`.ts` 翻译文件）
- [ ] 中文简体 + 英文作为首批语言
- [ ] 语言切换入口（菜单或设置）

### 9. 历史数据回放与导出

- [ ] 历史数据查询 UI（时间范围选择器 + 设备多选）
- [ ] 历史曲线回放（滚动/缩放/暂停/变速）
- [ ] 导出 CSV 文件
- [ ] 导出 PDF 报表（统计摘要 + 曲线截图）
- [ ] 设备利用率 / 良品率统计

### 10. 设备模板与批量管理

- [ ] 设备类型模型（型号 / 参数模板 / 报警阈值 / 采集频率）
- [ ] 设备批量导入（CSV）
- [ ] 设备分组管理（产线 / 工段）
- [ ] 设备数量上限由配置文件控制（当前硬编码 10）

### 11. 用户权限管理

- [ ] 登录界面（用户名 + 密码）
- [ ] 角色定义（操作员 / 工程师 / 管理员）
- [ ] 操作员：仅查看
- [ ] 工程师：查看 + 参数下发
- [ ] 管理员：查看 + 参数下发 + 修改配置 + 管理用户
- [ ] 操作日志（谁在什么时间做了什么操作）

---

## 工作量估算

| 优先级 | 任务 | 估计人天 |
|--------|------|---------|
| P0 | 自动化测试覆盖 | 3-5 |
| P0 | 配置文件化 | 2-3 |
| P0 | 部署自动化 | 0.5 |
| P1 | 报警管理完善 | 2-3 |
| P1 | 数据生命周期管理 | 1-2 |
| P1 | 日志覆盖关键路径 | 1-2 |
| P1 | 离线/断网完善 | 3-5 |
| P2 | 多语言支持 | 2-3 |
| P2 | 历史数据回放与导出 | 5-7 |
| P2 | 设备模板与批量管理 | 3-5 |
| P2 | 用户权限管理 | 3-5 |
